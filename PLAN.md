# 综合备忘录 · 细节打磨 & 功能增强 实施计划

> 单文件 HTML PWA（`index.html` + `sw.js` + `manifest.webmanifest`），本地 AES-GCM 加密，GitHub Pages 部署。
> 本计划基于 2026-08-02 的需求确认：部分建议剔除，其余纳入。

## 一、范围确认

### ✅ 纳入
**页面细节**
1. 锁屏精致化（输错抖动 + 红点、9 宫格点填充动画、锁屏渐变/磨砂背景）
2. 待办卡片：左侧时间色条（超时红 / 今天橙 / 未来灰 / 已完成绿）
3. 待办卡片：勾选完成动画（划线 + 淡出 + 打勾）、清空已完成纸屑动画
4. 统一 Design Token（`--radius` / `--gap` / `--shadow-1/2/3`）+ 生成 `DESIGN.md` 固化规范
5. 微交互：卡片 `:active` 缩放 0.98、顶栏滚动毛玻璃、FAB 滚动隐藏/显示
6. 时间感知问候语（早/午/晚 + 待办数提示）

**实用功能**
7. 数据导出备份（导出加密 `.json`）
8. 备忘置顶（pin）+ 回收站（最近 30 天可恢复）
9. 全局跨模块搜索（顶部一搜搜全部：待办/备忘/银行卡后四位/股票）
10. 系统通知提醒（接入 Notification API，到点弹系统通知）
11. 倒数日（并入待办模块：新增"设为倒数日"类型，显示"还有 N 天"，不加独立导航）
12. 银行卡增强：一键复制卡号、安全码默认遮罩点击显示
13. 待办优先级（高/中/低）+ 拖拽排序
14. 自动备份（stretch：本地自动导出 `.json`；网盘自动同步受 PWA 静态托管 + CORS 限制，建议沿用你现有百度网盘机制）

### ❌ 明确剔除
- 连续打卡天数 / 日历热力图
- 待办子任务
- 银行卡到期提醒
- 记账模块
- 倒数日独立模块（已改为并入待办）

---

## 二、分阶段路线图

### Phase 0 — 设计基座（低风险，纯整理）
- 提取全站 Design Token：`--radius`、`--gap`、`--shadow-1/2/3`、`--color-*` 等。
- 生成 `DESIGN.md`，固化现有 + 计划的设计规范，后续加模块不跑偏。
- 产出：CSS 变量集中定义 + `DESIGN.md`。

### Phase 1 — 微交互 & 锁屏精致化（低成本高回报）
- 锁屏输错：整个键盘 `shake` 动画 + 圆点变红。
- 9 宫格数字点：输入一位时 0.15s 缩放弹出的填充动画。
- 锁屏背景：极淡渐变 / 磨砂层。
- 卡片 `:active{transform:scale(.98)}` + 阴影变浅。
- 顶部标题栏滚动时叠 `backdrop-filter: blur()`。
- FAB：向下滚动隐藏、向上滚动浮现。
- 问候语改为时间感知：「晚上好，今天还有 3 件待办」。

### Phase 2 — 待办模块增强
- 卡片左侧彩色时间竖条（红/橙/灰/绿）。
- 勾选完成：文字划线 + 淡出 + 复选框打勾动画。
- 清空已完成：轻量纸屑 / 对勾动画。
- 优先级：高/中/低，列表按优先级 + 时间排序；长按拖动排序。
- **倒数日（并入待办）**：新建待办增加「设为倒数日」开关 → 显示「还有 N 天」倒计时徽章（今天/已过 N 天）；筛选 tab 增加「倒数」；完成动作改为「标记已发生」。

### Phase 3 — 备忘模块
- 置顶（pin）：列表顶部固定，带图钉标记。
- 回收站：删除进「最近 30 天可恢复」，误删可还原。

### Phase 4 — 银行卡模块
- 一键复制卡号（点击卡号 `navigator.clipboard`）。
- 安全码（CVV）默认遮罩 `•••`，点击显示 5 秒后自动隐藏。

### Phase 5 — 全局 & 数据
- 全局跨模块搜索：顶部搜索栏即为全局搜索入口（`onSearchInput` 触发 `openGlobalSearch`，聚合待办/备忘/银行卡/笔记结果，点击直达模块详情）；设置菜单不再单独保留「全局搜索」入口。
- 数据导出备份：设置菜单增加「导出备份」→ 下载加密 `.json`。
- 系统通知提醒：接入 Notification API，授权后到点弹系统通知（受手机端 PWA 系统支持限制，需告知用户）。
- 自动备份（stretch）：每次改动后静默本地导出 `.json`；网盘自动同步建议沿用现有百度网盘机制。

---

## 三、建议实施顺序

`0 → 1 → 2 → 3 → 4 → 5`

理由：Phase 0/1 改动集中、风险低、肉眼提升最大；Phase 2–4 是各模块功能增量；Phase 5 的全局搜索/导出/通知串联全站，放最后收口。

---

## 四、约束与注意
- 所有改动落在 `index.html`（单文件），部署沿用既有流程：打包 `memo-deploy.zip` → 覆盖到 `D:\备忘录\memo-app` → GitHub Desktop Commit + Push → 手机流量刷新。
- 加密逻辑（AES-GCM / PBKDF2 / 9 宫格 PIN）保持不变，新功能只动 UI 与数据模型。
- 新增数据字段（如待办 `priority`、备忘 `pinned`/`deletedAt`、倒数日 `countdown` 标记）需在 `normalizeState()` 中设默认值，保证老保险库向后兼容（参照上次的 todo 保存 bug 修复）。
- 自动备份到网盘受 GitHub Pages 静态托管 + CORS 限制，本期只做本地自动导出，网盘同步维持你现有百度网盘方案。

---

## 五、实施完成情况（截至 2026-08-02）

| 阶段 | 内容 | 状态 |
|------|------|------|
| Phase 0 | Design Token 提取 + `DESIGN.md` | ✅ 完成 |
| Phase 1 | 锁屏抖动/红点、9宫格填充动画、磨砂背景、卡片按压、顶栏滚动毛玻璃、FAB 隐显、时间问候语 | ✅ 完成 |
| Phase 2 | 待办左侧时间色条、完成/清空动画、优先级(高/中/低)、拖拽排序、倒数日并入待办 | ✅ 完成 |
| Phase 3 | 备忘置顶(pin) + 回收站(30天可恢复) | ✅ 完成 |
| Phase 4 | 银行卡一键复制卡号 + CVV 默认遮罩点击显示 | ✅ 完成 |
| Phase 5 | 全局跨模块搜索、数据导出、系统通知(Notification API)、本地自动备份 | ✅ 完成 |

### 本批次关键改动点
- **待办**：`todoStatusClass()` 输出 `s-over/s-today/s-soon/s-done` 驱动左侧色条；`priority` 高/中/低彩色徽章；`countdown` 倒数日徽章与「倒数」筛选 tab；`setupTodoDnD()` 拖拽重排（稳定排序，已完成沉底）；`clearDoneTodos()` 触发 `confettiBurst()` 纸屑。
- **备忘**：`pinned` 置顶 + 卡片图钉标记 + 列表置顶排序；删除改为移入 `state.trash`（带 `deletedAt`，`cleanupTrash()` 自动清理超 30 天），设置菜单新增「回收站」可恢复/彻底删除/清空。
- **银行卡**：卡号整行 `copyText()` 一键复制（含 `execCommand` 兜底）；CVV 经 `revealedCVV` Set 控制遮罩 `•••` ⇄ 明文，列表与详情双端一致。
- **全局**：`openGlobalSearch()` 跨 待办/备忘/银行卡/笔记 聚合结果，点击直达对应模块详情；设置新增「全局搜索」「系统通知」「自动备份」。
- **通知**：`requestNotify()` 申请权限、`notify()` 发系统通知；解锁时若已授权，弹「今日概览」汇总待办/超时/账单。
- **自动备份**：`save()` 每次静默写入 `localStorage('memo_auto_backup')`（剥离图片以控体积），`restoreAutoBackup()` 可一键恢复。
- **健壮性**：`normalizeState()` 新增 `trash:[]` 默认值，老保险库向后兼容。

### 验证
- 主脚本 `node --check` 通过（约 739 行）。
- 所有新增/引用函数（`openStockDetail`/`isOverdue`/`copyText`/`toggleCVV`/`openGlobalSearch`/`requestNotify`/`restoreAutoBackup` 等）均存在定义。
- 新增 CSS 变量（`--primary-soft`/`--primary-strong`/`--warning-strong`/`--r-sm`/`--r-pill` 等）在明/暗双主题均有定义。

### 追加调整（2026-08-02 18:23）
- **顶部搜索栏改为全局搜索**：`onSearchInput` 不再按当前模块内联过滤，改为触发跨模块聚合搜索（`openGlobalSearch` 复用同款结果弹层，支持带入初始关键词并即时渲染）；设置菜单移除冗余「全局搜索」入口；搜索框 placeholder 改为「全局搜索：待办 / 备忘 / 银行卡 / 笔记…」。

